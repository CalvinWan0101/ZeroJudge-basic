#include <iostream>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    string a, b;
    cin >> a >> b;
    cout << a << " and " << b << " sitting in the tree" << endl;
    return 0;
}
