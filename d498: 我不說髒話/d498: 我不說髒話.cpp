#include <iostream>
#include <string>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    while (cin >> n)
        for (int i = 1; i <= n; i++)
            cout << "I don't say swear words!" << endl;
    return 0;
}
