#include <iostream>
using namespace std;
int main(int argc, char** argv)
{
	int n;
	cin >> n;
	int before[30];
	int after[30];
	for (int i = 0; i < 30; i++)
		cin >> before[i];
	for (int i = 0; i < 30; i++)
		cin >> after[i];
	system("PAUSE");
	return 0;
}
